package com.loopnow.fireworkdemo

import com.loopnow.fireworklibrary.FireworkSDK
import com.loopnow.fireworklibrary.Key
import org.junit.Assert.*
import org.junit.Test

class UrlUnitTest {

    /*val staging = false
    val map = HashMap<String,Boolean>().apply {

        put(Key.VIDEO_EVENTS, false)
        put(Key.VISITOR_EVENTS, true )
    }

    @Test
    fun test_playSegmentStagingUrl() {
        val pixel2 = false
        val staging = true

        map.apply {
            put(Key.PLAY_SEGMENTS, pixel2 )
        }
        FireworkSDK.setUrls(staging, map)
        assertEquals(Key.PLAY_SEGMENTS_URL, Key.PIXEL_ONE_STAGING_URL )
    }

    @Test
    fun test_playSegmentProductionUrl() {
        val pixel2 = false
        val staging = false
        map.apply {
            put(Key.PLAY_SEGMENTS, pixel2 )
        }
        FireworkSDK.setUrls(staging, map)
        assertEquals(Key.PLAY_SEGMENTS_URL, Key.PIXEL_ONE_PRODUCTION_URL )
    }


    @Test
    fun test_playSegmentStagingUrl2() {
        val pixel2 = true
        val staging = true

        map.apply {
            put(Key.PLAY_SEGMENTS, pixel2 )
        }
        FireworkSDK.setUrls(staging, map)
        assertEquals(Key.PLAY_SEGMENTS_URL, Key.PIXEL_TWO_STAGING_URL )
    }

    @Test
    fun test_playSegmentProductionUrl2() {
        val pixel2 = true
        val staging = false
        map.apply {
            put(Key.PLAY_SEGMENTS, pixel2 )
        }
        FireworkSDK.setUrls(staging, map)
        assertEquals(Key.PLAY_SEGMENTS_URL, Key.PIXEL_TWO_PRODUCTION_URL )
    }


    @Test
    fun test_visitorEventStagingUrl() {
        val pixel2 = false
        val staging = true

        map.apply {
            put(Key.PLAY_SEGMENTS, pixel2 )
        }
        FireworkSDK.setUrls(staging, map)
        assertEquals(Key.PLAY_SEGMENTS_URL, Key.PIXEL_ONE_STAGING_URL )
    }

    @Test
    fun test_visitorEventsProductionUrl() {
        val pixel2 = false
        val staging = false
        map.apply {
            put(Key.PLAY_SEGMENTS, pixel2 )
        }
        FireworkSDK.setUrls(staging, map)
        assertEquals(Key.PLAY_SEGMENTS_URL, Key.PIXEL_ONE_PRODUCTION_URL )
    }


    @Test
    fun test_visitorEventsStagingUrl2() {
        val pixel2 = true
        val staging = true

        map.apply {
            put(Key.PLAY_SEGMENTS, pixel2 )
        }
        FireworkSDK.setUrls(staging, map)
        assertEquals(Key.PLAY_SEGMENTS_URL, Key.PIXEL_TWO_STAGING_URL )
    }

    @Test
    fun test_visitorEventsProductionUrl2() {
        val pixel2 = true
        val staging = false
        map.apply {
            put(Key.PLAY_SEGMENTS, pixel2 )
        }
        FireworkSDK.setUrls(staging, map)
        assertEquals(Key.PLAY_SEGMENTS_URL, Key.PIXEL_TWO_PRODUCTION_URL )
    }*/
}